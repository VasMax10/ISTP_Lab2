function onLeague() {
    var elem = document.getElementById('league');
    elem.setAttribute('style', `background-image : url('images/backgrounds/collections_button_leagues.png')`);
}

function leftLeague() {
    var elem = document.getElementById('league');
    elem.setAttribute('style', `background-image : url('images/backgrounds/collections_button_bw_leagues.png')`);
}

function onNation() {
    var elem = document.getElementById('nation');
    elem.setAttribute('style', `background-image : url('images/backgrounds/collections_button_nations.png')`);
}

function leftNation() {
    var elem = document.getElementById('nation');
    elem.setAttribute('style', `background-image : url('images/backgrounds/collections_button_bw_nations.png')`);
}

function onCard() {
    var elem = document.getElementById('card');
    elem.setAttribute('style', `background-image : url('images/backgrounds/collections_button_cards.png')`);
}

function leftCard() {
    var elem = document.getElementById('card');
    elem.setAttribute('style', `background-image : url('images/backgrounds/collections_button_bw_cards.png')`);
}